using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;

public class MobileData : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Stream s; 
        //DataSender sendData = new DataSender(stream: s);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
